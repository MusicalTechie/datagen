
The python script DataGen_1220_Generate_items_initial.py creates an initial set of randomly generated ITEM names that have a prefixes determined by the array DG_ITEMS where an incrementing number is added as a suffix to each prefix, resulting in a unique ITEM ID.

The python script DataGen_1012_Main_DataGen_Delta.py needs to be modified to check for the existence of the files described by v_parm_path_items_merged, v_parm_path_customers_merged, v_parm_path_organizations_merged, and v_parm_path_matrix_merged.  If the file v_parm_path_items_merged does not exist, create one by copying the file file v_parm_path_items_base. If the file v_parm_path_customers_merged does not exist, create one by copying the file file v_parm_path_customers_base. If the file v_parm_path_organizations_merged does not exist, create one by copying the file file v_parm_path_organizations_base. If the file v_parm_path_matrix_merged does not exist, create one by copying the file file v_parm_path_matrix_base.  If any of the files described by the variables  v_parm_path_customers_base,  v_parm_path_customers_base,  v_parm_path_customers_base,  v_parm_path_customers_base do NOT exist, then exit the script with error message indicating which file does not exist and noting existence of the file is mandatory for any delta data generation.

The python script DataGen_1230_Generate_customers_initial.py creates an initial set of randomly generated CUSTOMER names that have a prefixes determined by the array DG_CUSTOMERS where an incrementing number is added as a suffix to each prefix, resulting in a unique CUSTOMER ID.

Before creating a new "ITEM" in the script DataGen_1320_Generate_items_delta.py, the existing merged ITEMs file noted in v_parm_path_items_merged, needs to opened, sorted by "Item" column (fourth column).  For each "Item" and each unique prefix, the highest number value needs to be determined and then the next higher number can be used to create the "new ITEM for the new time period".

Before creating a "CUSTOMER" in the DataGen_1330_Generate_customers_delta.py, the existing merged CUSTOMERs file noted in v_parm_path_customers_merged, needs to opened, sorted by "ShipToLocation" column (fifth column).  For each "Customer" and each unique prefix, the highest number value needs to be determined and then the next higher number can be used to create the "new CUSTOMER for the new time period".

Before creating a new MATRIX row (a new ORGANIZATION, ITEM, and CUSTOMER "combination") in the script DataGen_1340_Generate_matrix_delta.py, the values from the consolidated files v_parm_path_items_merged (for ITEMs), v_parm_path_customers_merged (for CUSTOMERS), and v_parm_path_organizations_merged (for ORGANIZATIONS; this file is currently not used and a fixed organization of "002" is being used throughout all the scripts) have to be loaded, the new MATRIX row key ("Item" and "Location") must NOT already exist in either of the files v_parm_path_matrix_merged or v_parm_path_matrix_delta; only if the new Matrix row key is unique across the two files should the new row be created in v_parm_path_matrix_delta.

Assess the existing scripts noted in this query and determine how they can be modified to achieve the objectives in the query.
